import {products,productCategories,serviceCategories} from './data.js';
import {getSelected,addProduct,removeProduct,setSelected} from './state.js';
import {button,escapeHTML,toast} from './ui.js';
import {withBase} from './config.js';
import {validateQuote,validateFiles} from './validation.js';
import {submitQuote} from './quote-api.js';
const field=(id,label,type='text',required=false,autocomplete='')=>`<div class="field"><label for="${id}">${label}${required?' <span aria-hidden="true">*</span>':''}</label><input id="${id}" name="${id}" type="${type}" ${required?'required':''} ${autocomplete?`autocomplete="${autocomplete}"`:''} aria-describedby="${id}-error"><p id="${id}-error" class="field-error"></p></div>`;
export function contactPage(){return `<section class="page-intro container quote-intro"><div><p class="label">Let’s talk about your outdoors</p><h1>A fresh start, one simple step.</h1></div><p>A few details are all it takes to start shaping your project. Big ideas and small improvements are equally welcome.</p></section><section class="container quote-layout"><form id="quote-form" novalidate><div class="form-section"><div class="form-heading"><h2>A little about you</h2><p>Fields marked * are required.</p></div><div class="form-grid">${field('name','Full name','text',true,'name')}${field('email','Email address','email',true,'email')}${field('phone','Phone number','tel',true,'tel')}<div class="field"><label for="preferred">Preferred contact method <span aria-hidden="true">*</span></label><select id="preferred" name="preferred" required aria-describedby="preferred-error"><option value="">Select a preference</option><option value="email">Email</option><option value="phone">Phone call</option><option value="text">Text message</option></select><p id="preferred-error" class="field-error"></p></div><div class="field full"><label for="address">Address or project location</label><input id="address" name="address" autocomplete="street-address" placeholder="Street address, neighborhood, or area"></div></div></div><div class="form-section"><div class="form-heading"><h2>Your project, your way</h2><p>Select what you have in mind. We can work out the rest.</p></div><fieldset class="property-type"><legend>Property type</legend><label><input type="radio" name="property" value="Residential" checked> Residential</label><label><input type="radio" name="property" value="Commercial"> Commercial</label></fieldset><fieldset class="service-picker"><legend>Services you’re interested in <span>Select all that apply</span></legend>${serviceCategories.map(c=>`<details><summary>${c.name}<span>+</span></summary><div class="checkbox-grid">${c.services.map(s=>`<label><input type="checkbox" name="services" value="${s}"><span>${s}</span></label>`).join('')}</div></details>`).join('')}</fieldset><div class="selected-materials"><h3>Selected materials</h3><p>Favorites from the catalog appear here.</p><div id="selected-materials-list"></div><div class="material-select-row"><label class="sr-only" for="add-material-select">Choose a material to add</label><select id="add-material-select"><option value="">Choose a material…</option>${productCategories.map(c=>`<optgroup label="${c.name}">${products.filter(p=>p.category===c.id).map(p=>`<option value="${p.id}">${p.name}</option>`).join('')}</optgroup>`).join('')}</select><button type="button" class="button" id="add-material">Add</button></div></div><div class="field"><label for="description">Tell us about your project</label><textarea id="description" name="description" rows="5" maxlength="5000" placeholder="What would you like to change, improve, or maintain?" aria-describedby="description-hint description-error"></textarea><p id="description-hint" class="field-hint">Include a description or select at least one service or material.</p><p id="description-error" class="field-error"></p></div><div class="form-grid"><div class="field"><label for="size">Approximate project size</label><input id="size" name="size" placeholder="e.g. 1/4 acre, 200 sq ft, or not sure"></div><div class="field"><label for="timeframe">Desired timeframe</label><select id="timeframe" name="timeframe"><option value="">Select a timeframe</option><option>As soon as practical</option><option>Within a month</option><option>In 1–3 months</option><option>Just exploring</option><option>Recurring maintenance</option></select></div><div class="field"><label for="budget">Budget range <span class="optional">(optional)</span></label><select id="budget" name="budget"><option value="">Prefer to discuss</option><option>Under $1,000</option><option>$1,000–$5,000</option><option>$5,000–$15,000</option><option>$15,000+</option></select></div><div class="field"><label for="referral">How did you hear about us?</label><select id="referral" name="referral"><option value="">Select an option</option><option>Portfolio</option><option>Search engine</option><option>Friend or neighbor</option><option>Social media</option><option>Other</option></select></div></div></div><div class="form-section"><div class="form-heading"><h2>Help us picture it</h2><p>Optional photos of your property or project.</p></div><div class="upload-zone" id="upload-zone"><span aria-hidden="true">↥</span><label for="photos">Choose photos <span>or drag them here</span></label><input id="photos" type="file" accept="image/jpeg,image/png,image/webp" multiple aria-describedby="photos-hint photos-error"><p id="photos-hint">JPG, PNG, or WebP · Up to 5 images · 10 MB each</p></div><p class="field-error" id="photos-error" role="alert"></p><div id="photo-previews" class="photo-previews"></div><button type="button" class="clear-photos" id="clear-photos">Clear photos &amp; upload errors</button></div><div class="form-submit"><p class="demo-note"><b>A note about this demo</b>This form simulates a quote request. Your information and photos stay in your browser and are not sent to a business.</p><p id="form-errors" class="field-error" role="alert" tabindex="-1"></p><button type="submit" class="button" id="submit-quote">Request my quote</button><p class="small-copy">No commitment. Just a conversation about what’s possible.</p></div></form><aside class="quote-aside"><div class="quote-guide"><h2>From a little idea<br>to a lovely outdoors.</h2><p>You don’t need a finished plan to get started.</p><ol><li><b>Share the basics</b><span>Tell us what you’re thinking, and add any materials you like.</span></li><li><b>Talk it through</b><span>A representative would normally review your project and arrange a conversation.</span></li><li><b>Know what comes next</b><span>A site review helps shape a clear scope and a tailored estimate.</span></li></ol><p class="small-copy">Your shortlist is saved on this device. Personal details and photos are not saved.</p></div><div class="quote-tip"><p class="label">A good place to start</p><p>Unsure of quantities or plant varieties? Tell us how you’d like the space to feel. The details can come later.</p></div></aside></section>`;}
export function initContact(){const form=document.querySelector('#quote-form');let files=[];let previewUrls=[];const selectedList=document.querySelector('#selected-materials-list');function renderMaterials(){const ids=getSelected();selectedList.innerHTML=ids.length?ids.map(id=>`<span class="material-chip">✓ ${products.find(p=>p.id===id).name}<button type="button" data-remove-material="${id}" aria-label="Remove ${products.find(p=>p.id===id).name}">×</button></span>`).join(''):`<p class="empty-materials">No materials selected yet. <a href="${withBase('/products/')}">Explore the catalog</a></p>`;selectedList.querySelectorAll('button').forEach(el=>el.onclick=()=>{const index=[...selectedList.querySelectorAll('button')].indexOf(el);removeProduct(el.dataset.removeMaterial);const next=selectedList.querySelectorAll('button');(next[Math.min(index,next.length-1)]||document.querySelector('#add-material-select')).focus();});}document.addEventListener('materialschange',renderMaterials);renderMaterials();document.querySelector('#add-material').onclick=()=>{const select=document.querySelector('#add-material-select');if(select.value){addProduct(select.value);toast('Material added to your quote.');select.value='';}else{select.focus();toast('Choose a material first.');}};const requested=new URLSearchParams(location.search).getAll('service');form.querySelectorAll('[name=services]').forEach(el=>{if(requested.includes(el.value)){el.checked=true;el.closest('details').open=true;}});
 const photos=document.querySelector('#photos');const photoError=document.querySelector('#photos-error');function renderPhotos(){previewUrls.forEach(URL.revokeObjectURL);previewUrls=[];document.querySelector('#photo-previews').innerHTML=files.map((file,i)=>{const url=URL.createObjectURL(file);previewUrls.push(url);return `<div><img src="${url}" alt="Selected project photo ${i+1}"><span>${escapeHTML(file.name)}</span><button type="button" data-remove-photo="${i}" aria-label="Remove photo ${escapeHTML(file.name)}">×</button></div>`;}).join('');document.querySelectorAll('[data-remove-photo]').forEach(el=>el.onclick=()=>{files.splice(Number(el.dataset.removePhoto),1);photoError.textContent='';renderPhotos();photos.focus();});}
 async function acceptFiles(incoming){const list=Array.from(incoming);const error=validateFiles(list,files.length);if(error){photoError.textContent=error;photos.setAttribute('aria-invalid','true');photos.value='';return;}const valid=await Promise.all(list.map(file=>new Promise(resolve=>{const url=URL.createObjectURL(file);const img=new Image();img.onload=()=>{URL.revokeObjectURL(url);resolve(true);};img.onerror=()=>{URL.revokeObjectURL(url);resolve(false);};img.src=url;})));if(valid.some(x=>!x)){photoError.textContent='One or more files could not be opened as images. Please choose valid photos.';photos.setAttribute('aria-invalid','true');photos.value='';return;}const secondError=validateFiles(list,files.length);if(secondError){photoError.textContent=secondError;photos.value='';return;}files.push(...list);photoError.textContent='';photos.removeAttribute('aria-invalid');photos.value='';renderPhotos();}
 document.querySelector('#clear-photos').onclick=()=>{files=[];photoError.textContent='';photos.removeAttribute('aria-invalid');photos.value='';renderPhotos();photos.focus();};photos.onchange=()=>acceptFiles(photos.files);const zone=document.querySelector('#upload-zone');zone.ondragover=e=>{e.preventDefault();zone.classList.add('dragover');};zone.ondragleave=()=>zone.classList.remove('dragover');zone.ondrop=e=>{e.preventDefault();zone.classList.remove('dragover');acceptFiles(e.dataTransfer.files);};window.addEventListener('pagehide',()=>previewUrls.forEach(URL.revokeObjectURL));
 form.addEventListener('input',e=>{if(e.target.name && document.getElementById(e.target.name+'-error')){document.getElementById(e.target.name+'-error').textContent='';e.target.removeAttribute('aria-invalid');}});
 form.onsubmit=async e=>{e.preventDefault();const fd=new FormData(form);const data=Object.fromEntries(fd);data.services=fd.getAll('services');data.products=getSelected();data.photos=files;const errors=validateQuote(data);for(const key of ['name','email','phone','preferred','description']){const field=form.elements[key];field.setAttribute('aria-invalid',String(!!errors[key]));document.getElementById(key+'-error').textContent=errors[key]||'';}const summary=document.querySelector('#form-errors');if(Object.keys(errors).length){summary.textContent=`Please check ${Object.keys(errors).length} ${Object.keys(errors).length===1?'field':'fields'} highlighted above.`;document.getElementById(Object.keys(errors)[0]).focus();return;}if(photoError.textContent){summary.textContent='Please resolve the photo error or remove the invalid selection.';photos.focus();return;}summary.textContent='';const submit=document.querySelector('#submit-quote');submit.disabled=true;submit.textContent='Preparing your request…';try{await submitQuote(data);const chosenNames=data.products.map(id=>products.find(p=>p.id===id).name);form.innerHTML=`<div class="success-state" tabindex="-1"><span class="success-icon">✓</span><p class="label">Demo request complete</p><h2>Your request has been received.</h2><p>Thank you, ${escapeHTML(data.name.trim().split(' ')[0])}. A landscaping representative would normally review your project details and contact you by ${data.preferred==='phone'?'phone':data.preferred==='text'?'text message':'email'} to discuss the next steps.</p><div class="success-summary"><h3>Your project summary</h3><p>${escapeHTML(data.property)}${data.services.length?' · '+data.services.length+' services selected':''}${chosenNames.length?' · '+chosenNames.length+' materials selected':''}${files.length?' · '+files.length+' photos attached':''}</p>${chosenNames.length?`<p>${chosenNames.map(escapeHTML).join(' · ')}</p>`:''}</div><p class="demo-note">This was a simulated submission. No information or images were sent, and no representative will contact you.</p>${button('Back to the garden',withBase('/'))}<button type="button" class="text-link" id="start-again">Start a new request</button></div>`;previewUrls.forEach(URL.revokeObjectURL);previewUrls=[];files=[];setSelected([]);document.querySelector('.success-state').focus();document.querySelector('#start-again').onclick=()=>location.assign(withBase('/contact/'));}catch{summary.textContent='Something went wrong while preparing the demo. Your details are still here. Please try again.';submit.disabled=false;submit.textContent='Request my quote';}};
}
